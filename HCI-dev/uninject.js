/**
 * Created by Josh on 3/16/2015.
 */
if (window.top === window) {

    // Inject the bridge script
    var scheduleScript = document.getElementById('scheduleScript');

    scheduleScript.remove();


}